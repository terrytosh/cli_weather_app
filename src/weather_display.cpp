#include "../includes/weather_display.h"
#include <iostream>

void WeatherDisplay::displayWeather(std::string data) {
    // Implement logic to display the weather data
    std::cout << "Displaying weather data: " << data << std::endl;
}